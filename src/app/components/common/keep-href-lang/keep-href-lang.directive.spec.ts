import { KeepHrefLangDirective } from './keep-href-lang.directive';

describe('KeepHrefLangDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepHrefLangDirective();
    expect(directive).toBeTruthy();
  });
});
