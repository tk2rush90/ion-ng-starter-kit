import { GoogleAccessTokenHandlerDirective } from './google-access-token-handler.directive';

describe('GoogleOauthHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleAccessTokenHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
